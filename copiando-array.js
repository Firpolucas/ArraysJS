const notas = [5, 7, 8, 9];

const notasAtualizadas = [...notas, 22];    //... copia exatamente os dados que estao dentro de notas para notasAtualizadas e insere o 22 no final

console.log(notasAtualizadas);